name: Email Verification Action
on: 
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - name: 23f3001101@ds.study.iitm.ac.in
        run: echo "Triggering action for verification"
